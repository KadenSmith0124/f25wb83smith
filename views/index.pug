extends layout

block content
  h1= title
  p Hello Hello Hello
  button(type="button", class="btn btn-secondary", onclick="faster()") Press me
  div(id="faster") add in extra
  script.
    let value = 1;
    let amt = 2;

    function faster() {
      amt = amt * 2;
      value = value + amt;
      document.getElementById("faster").innerHTML = "Faster " + value + " times " + amt;
    }

  a(href="/spells") View Spells
